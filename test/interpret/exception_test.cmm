int[10] a;
float b=1.5;
string c;

//错误测试
//类型不匹配
a[6] = "sadfsa";//字符串赋给数值变量
c = a[1];//数值赋给字符串
a =234;
out(d);//使用未声明变量
float b;//同一层级声明同名变量
out(a[c]);//使用字符串作为下标
a[6] = b+c;//字符串参与运算
a[6] = 12 / 0;//除数为负数
a[11] = 45;//数组越界
a[c] =45;//使用字符串变量作为下标
a[b] = 45;//使用浮点数作为下标
out("try to input a num to a string variable!");
in(c);//对字符串变量输入数字 如123
out("try to input a string to a num variable!");
in(b);//对数值变量输入字符串 如"123b"